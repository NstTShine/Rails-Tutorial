<li>
  <%= gravatar_for user, size: Settings.users_view.gravatar_profile %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to (t ".delete_link"), user, method: :delete,
    data: {confirm: (t ".delete_confirm")} %>
  <% end %>
</li>
